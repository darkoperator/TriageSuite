# Evidence test captures — never committed
test captures

# Large NTFS sample fixtures for triage-mft unit tests (copied locally, never committed)
sample_files/

# Rust build artifacts
/target
**/*.rs.bk

# .NET build artifacts (scripts/guid_extractor)
**/obj/
**/bin/

# Local tool output
/output/
/results/
.worktrees/

# macOS
.DS_Store
/dist/
